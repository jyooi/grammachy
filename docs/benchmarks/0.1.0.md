# Grammachy benchmark 0.1.0

Fixture: 30 interference sentences and 10 correct ones, `cli/tests/fixtures/interference-30.json`.
Machine: 16 GB tier, 24 CPUs, 27 GB RAM.
Command: `grammachy bench --engine openai --model qwen2.5-7b-instruct --model qwen2.5-3b-instruct`.

## Engines

| Engine | Catch rate | False positives | p50 latency | Resident memory |
|---|---|---|---|---|
| `languagetool` | 10 of 30 (33%) | 0 of 10 | 18 ms | 775 MB |
| `harper` | 5 of 30 (17%) | 0 of 10 | 13 ms | 302 MB |
| `openai` | skipped | skipped | skipped | skipped |

Resident memory of `languagetool` is the RSS of its server process.
Resident memory of `harper` is the growth of this process's own peak RSS, because it runs in process.
Resident memory of `openai` is the RSS of its server process.

## Models

| Model | Catch rate | False positives | p50 latency | Resident memory | Weights license | Recommended |
|---|---|---|---|---|---|---|
| `qwen2.5-7b-instruct` | skipped | skipped | skipped | skipped | Apache-2.0 | eligible |
| `qwen2.5-3b-instruct` | skipped | skipped | skipped | skipped | Qwen Research License | never, the weights are non-commercial |

The recommended model of the Settings defaults and the README is the best row marked `eligible` that fits the machine tier above (spec section 13.1).

## Skipped

These rows did not run on this machine. A row that is not reachable is skipped, never an error.

- Engine `openai`: llama.cpp is not installed: /usr/bin/llama-server does not exist. Install it with: sudo pacman -S llama-cpp ggml-cpu   (add ggml-vulkan for a GPU or an iGPU) (at fixture sentence zh-01)
- Model `qwen2.5-7b-instruct`: llama.cpp is not installed: /usr/bin/llama-server does not exist. Install it with: sudo pacman -S llama-cpp ggml-cpu   (add ggml-vulkan for a GPU or an iGPU) (at fixture sentence zh-01)
- Model `qwen2.5-3b-instruct`: llama.cpp is not installed: /usr/bin/llama-server does not exist. Install it with: sudo pacman -S llama-cpp ggml-cpu   (add ggml-vulkan for a GPU or an iGPU) (at fixture sentence zh-01)

## Regression rule

A release must not drop the catch rate of the default engine, `languagetool`, and must not raise its false positives, against the previous file in `docs/benchmarks/`. A row that was skipped in one file and measured in the next is a new measurement, not a regression.

## How the numbers are measured

- Catch rate: an interference sentence is caught when at least one Issue overlaps the span the fixture expects. A right span with a wrong Fix still counts, because the Panel shows the span and lets the user Skip the Fix.
- False positives: correct sentences that earned at least one Issue. One sentence counts once, however many Issues it earned.
- p50 latency: the median over every sentence of the fixture, correct ones included, measured in process around one Check.
- Every sentence is checked with the Native language the fixture records for it, which is what the shell passes on a real Check.

